<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-12">
    <section class="user_info">
      <h1>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-12 text-center">
    <% if @user.works.any? %>
      <ol class="works list-unstyled">
        <%= render @works %>
      </ol>
      <%= will_paginate @works %>
    <% end %>
  </div>
</div>